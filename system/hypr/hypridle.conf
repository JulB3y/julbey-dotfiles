general {
    before_sleep_cmd = #lock before suspend.
    after_sleep_cmd =  hyprlock --no-fade-in & hyprctl dispatch dpms on  & filen sync /home/julbey/Documents:tw:Documents /home/julbey/Repos:lb:Repos # to avoid having to press a key twice to turn on the display.

}

listener {
    timeout = 150                                # 2.5min.
    on-timeout = brightnessctl -s set 10         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # monitor backlight restore.
}

listener {
    timeout = 600                                 # 10min
    on-timeout = hyprlock                         # lock screen when timeout has passed
}

listener {
    timeout = 300                                                     # 5min
    on-timeout = hyprctl dispatch dpms off                            # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}
